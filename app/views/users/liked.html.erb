<div class="row mb-4">
  <div class="col-md-6 offset-md-3">
    <%= render "users/user", user: @user %>
  </div>
</div>

<% if policy(@user).show_likes? %>
  <%= render "users/photos_tab", user: @user, photos: @user.liked_photos %>
<% end %>
